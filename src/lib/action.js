//SERVE ACTIONS


export const sayHello = async () =>{
    // to make this function a server directive 
    // we are going to use "use server"
    "use server" // at this poiny whatever is done in that function is run on the server 
}